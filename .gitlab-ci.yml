---
stages:
  - build

#rpm-build:
#  stage: build
#  image: centos:7
#  before_script:
#    - yum -y install
#        epel-release
#        git
#        rpm-build
#        rpmdevtools
#        make
#  script:
#    - mkdir -p rpmbuild/{SOURCES,RPMS,SRPMS}
#    - spectool -g -C rpmbuild/SOURCES build/bouncer.spec
#    - rpmbuild --define "_topdir `pwd`/rpmbuild" -bb build/bouncer.spec
#  artifacts:
#    paths:
#      - "*.rpm"
variables:
  OS: linux
  ARCH: amd64
  VERSION: 0.0.1
  GOPATH: ${CI_PROJECT_DIR}/dist

build-go:
  stage: build
  image: golang:1.14.3-alpine
  before_script:
    - go mod vendor
  script:
    - build/build.sh
  after_script:
    - mkdir -p ${GOPATH}/etc
    - cp config/bouncer.config.exmaple ${GOPATH}/etc/bouncer.yaml
  artifacts:
    paths:
      - ${GOPATH}/bin/bouncer
      - ${GOPATH}/etc/bouncer.yaml

build-rpm:
  stage: build
  image: centos:7
  script:
    - ls -la ${GOPATH}/bin/
    - ls -la ${GOPATH}/etc/
